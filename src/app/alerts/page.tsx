'use client'
import { useMemo, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Search } from 'lucide-react'
import { alerts, formatDate, severityClass, statusClass, type Alert } from '@/lib/data'
export default function AlertsPage(){ const [q,setQ]=useState(''); const [selected,setSelected]=useState<Alert|null>(null); const shown=useMemo(()=>alerts.filter(a=>`${a.inc_id} ${a.title} ${a.description}`.toLowerCase().includes(q.toLowerCase())),[q]); return <main className="min-h-screen bg-[#07111f] p-5 text-slate-100 sm:p-8"><div className="mx-auto max-w-[1500px]"><Link href="/" className="mb-6 inline-flex items-center gap-2 text-sm text-slate-400 hover:text-sky-300"><ArrowLeft/> Dashboard</Link><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs uppercase tracking-[0.16em] text-sky-400">Inventario operativo</p><h1 className="text-2xl font-semibold">Todas las alertas</h1><p className="mt-1 text-sm text-slate-500">{shown.length} de {alerts.length} alertas</p></div><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar por INC, título o descripción…" className="h-10 w-full rounded-lg border border-white/10 bg-[#0d1b2e] pl-10 pr-3 text-sm outline-none sm:w-80"/></div></div><div className="overflow-x-auto rounded-xl border border-white/10 bg-[#0d1b2e]"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-white/10 text-[11px] uppercase tracking-wider text-slate-500"><tr>{['INC','Título','Tipo','Severidad','Estado','Equipo','Detección'].map(h=><th key={h} className="px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-white/5">{shown.map(a=><tr key={a.inc_id} onClick={()=>setSelected(a)} className="cursor-pointer text-slate-300 hover:bg-white/[0.03]"><td className="px-4 py-3 font-mono text-xs text-sky-400">{a.inc_id}</td><td className="max-w-[260px] truncate px-4 py-3">{a.title}</td><td className="px-4 py-3 text-xs text-slate-400">{a.type}</td><td className="px-4 py-3"><span className={`rounded border px-2 py-1 text-[10px] ${severityClass[a.severity]}`}>{a.severity}</span></td><td className="px-4 py-3"><span className={`rounded border px-2 py-1 text-[10px] ${statusClass[a.status]}`}>{a.status}</span></td><td className="px-4 py-3 text-xs">{a.hostname.join(', ')}</td><td className="px-4 py-3 text-xs text-slate-500">{formatDate(a.detected_at)}</td></tr>)}</tbody></table></div></div>{selected&&<aside className="fixed inset-y-0 right-0 w-full max-w-lg overflow-y-auto border-l border-white/10 bg-[#0a1626] p-6 shadow-2xl"><button onClick={()=>setSelected(null)} className="mb-6 text-sm text-slate-400 hover:text-white">Cerrar detalle</button><div className="font-mono text-xs text-sky-400">{selected.inc_id}</div><h2 className="mt-2 text-xl font-semibold">{selected.title}</h2><p className="mt-5 text-sm leading-relaxed text-slate-300">{selected.description}</p><div className="mt-6 grid grid-cols-2 gap-5 text-xs"><div><span className="text-slate-600">Equipos</span><p className="mt-1">{selected.hostname.join(', ')}</p></div><div><span className="text-slate-600">Usuarios</span><p className="mt-1">{selected.username.join(', ')}</p></div><div><span className="text-slate-600">IPs</span><p className="mt-1">{selected.ip.join(', ')}</p></div><div><span className="text-slate-600">Grupo</span><p className="mt-1">{selected.group.join(', ')}</p></div></div><div className="mt-6 border-t border-white/10 pt-5 text-xs"><div className="text-slate-500">SHA256</div><p className="mt-2 break-all font-mono text-slate-400">{selected.file_hash}</p><a className="mt-4 inline-block text-sky-400" href={selected.url}>Abrir en portal SOC →</a></div></aside>}</main> }
